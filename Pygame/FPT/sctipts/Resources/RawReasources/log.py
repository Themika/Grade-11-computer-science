from Resources.resource import Resource

class Log(Resource):
    def __init__(self, x, y, images_folder, spawn_animation_frames, final_image):
        super().__init__(x, y, images_folder, spawn_animation_frames, final_image)